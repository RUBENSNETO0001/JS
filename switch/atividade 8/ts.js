/*var contador =30;
while (contador <= 0) {
    document.writeln(contador);
    contador++;
}

var contador = 30;
for (contador; contador >=0;contador--){

    document.writeln(contador + "<BR>");
}

var contador =30;
while (contador >= 0) {
    document.writeln(contador);
    contador--;
}

var nome = (prompt("qual e seu nome?"))

while(nome; nome = 5; ){
    document.writeln(nome+ "<BR>");
}
var nome = 0;
while (nome <= 5) {
    document.writeln("rubens");
    nome++;
}*/

var contador = 1;
while (contador <= 70) {
    document.writeln(contador);
    contador = contador + 2;
}